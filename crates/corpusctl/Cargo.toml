[package]
name = "corpusctl"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Builds and prunes a SKILL.md corpus: parallel walk, BLAKE3 dedup, supervised git clones"

[dependencies]
blake3 = "1"
rayon = "1"
walkdir = "2"

[lints]
workspace = true
